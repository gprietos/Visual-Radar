# viewer (placeholder)

Reserved for a future PyQt5-based GUI viewer. Scope is not yet decided —
whether it's a simple live display of `camera_view`/`radar_view` side by
side, or includes interactive controls (thresholds, toggles, pause). No
interface is stubbed here until that scope is confirmed.
